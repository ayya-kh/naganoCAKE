<div class="container">
  <div class="row">
    <div class="col-12">
      <h3 class="mb-4">注文履歴一覧</h3>
      <table class="table table-bordered">
        <thead>
          <tr class="thead-light">
            <th>注文日</th>
            <th>配送先</th>
            <th>注文商品</th>
            <th class="text-center">支払金額</th>
            <th class="text-center">ステータス</th>
            <th class="text-center">注文詳細 </th>
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |orders| %>
            <tr>
              <td class="text-center"><%= orders.created_at.strftime("%Y/%m/%d") %></td>
              <td>
                〒<%= orders.postal_code %><br>
                <%= orders.address %><br>
                <%= orders.name %>
              </td>
              <td>
                <% orders.order_details.each do |order_details| %>
                  <%= order_details.item.name %><br>
                <% end %>
              </td>
              <td class="text-center"><%= orders.total_payment.to_s(:delimited) %>円</td>
              <td class="text-center"><%= orders.status_i18n %></td>
              <td class="text-center"><%= link_to "表示する", order_path(orders), class: "btn btn-primary" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>